import 'package:flutter/material.dart';

class GearStats extends StatelessWidget {
  const GearStats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
