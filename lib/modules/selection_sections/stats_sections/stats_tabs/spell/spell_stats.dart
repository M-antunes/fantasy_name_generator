import 'package:flutter/material.dart';

class SpellStats extends StatelessWidget {
  const SpellStats({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}